# Field transformations for elita session logs (deprecated - now using search-time extraction)
# Location: $SPLUNK_HOME/etc/apps/elita_session/local/transforms.conf
#
# All field extraction is now handled via EXTRACT- and EVAL- statements in props.conf
# for search-time processing. Index-time TRANSFORMS- are no longer used.
