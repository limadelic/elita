name: Test

on: workflow_call

jobs:
  test:
    runs-on: ubuntu-latest
    timeout-minutes: 10

    steps:
      - uses: actions/checkout@v4

      - uses: ./.github/workflows/setup-beam.yml

      - name: Run tests
        run: mix test
        env:
          MIX_ENV: test
